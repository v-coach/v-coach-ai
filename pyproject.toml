[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "vcoach-ai"
version = "0.1.0"
description = "Food-101 classification training and TFLite export project for V-Coach"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]

